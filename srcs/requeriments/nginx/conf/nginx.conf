events {
	worker_connections 1024;
}
http {
	server {
		listen 443 ssl;
		server_name jortiz-m.42.fr;

		ssl_certificate /etc/ssl/certs/nginx.crt;
		ssl_certificate_key /etc/ssl/private/nginx.key;
		ssl_protocols TLSv1.3;
	}
}